on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: try job
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Hello world action step
        uses: ./ # Uses an action in the root directory
        id: hello
        with:
          prettier_command: 'git diff'

